import { SortDirectiveDirective } from './sort-directive.directive';

describe('SortDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SortDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
